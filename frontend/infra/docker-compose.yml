services:
  web:
    container_name: jogo-popcorn-web
    restart: always
    cpu_shares: 2
    mem_limit: 250m
    image: gsachetto/jogo-popcorn-web:1.0
    networks:
      - infra_external
    ports:
      - ${PORT}:80
      
networks:
  infra_external:
    external: true